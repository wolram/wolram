# WOLRAM configuration file
# Copy this file to wolram.toml and edit as needed.
# API key can also be set via ANTHROPIC_API_KEY env var (takes precedence).

# api_key = "sk-ant-..."

# Default model tier: haiku, sonnet, opus
default_model_tier = "sonnet"

# Maximum retries before marking a job as failed
max_retries = 3

# Base delay in milliseconds for exponential backoff
base_delay_ms = 1000
